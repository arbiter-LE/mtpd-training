# Internal files — tracked in git but must NEVER deploy to the public site.
# Anything not listed here that gets committed WILL be publicly fetchable
# on every *.arbiterle.com subdomain. When adding a new internal folder,
# add it here first.
CLAUDE.md
ARBITER-LE-PLAYBOOK.md
_archive
_database
_supabase
_legal
_legal-docs
_dev
_guide_build
_marketing
Agency Onboarding
EGPD-Module-Review.pdf

# Dated working notes are internal by default — brand docs, migration plans,
# starter prompts, meeting notes. They live loose in root but must never deploy.
# (email-signature.html and the brand/migration docs now live under _marketing
# and _archive, which are already ignored above.)
2026-*.md

# Office documents are internal by default — never deploy contracts,
# agreements, applications, or guides as public downloads.
*.docx
